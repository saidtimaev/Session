{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

    <h1>Formations : </h1>

    {% for formation in formations %}
        <a href="{{ path('sessionsParFormation', {'id': formation.id}) }}">{{formation.intitule}}</a><br>
    {% endfor %}

{% endblock %}
